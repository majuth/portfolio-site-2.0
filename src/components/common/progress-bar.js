const ProgressBar = () => {
    return(
    <span 
        style={{ transform: `translateX(${50-100}%)` }}
        className="fixed bg-white h-1 w-full top-0"
    />
    )
}

export default ProgressBar;